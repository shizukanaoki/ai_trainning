When finishing a task:
- Run npm run lint (if changes affect TS/TSX).
- Run npm run build if build validity matters.
- Manually verify UI via npm run dev/preview if styling or UI logic changed.
- Note: No automated tests configured.